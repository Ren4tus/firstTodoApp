# Spring Data Source ?? - MySQL DB ?? ?? ??

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/exampledatabase?useSSL=false&allowPublicKeyRetrieval=true&ServerTimezone=Asia/Seoul
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
#    database-platform: org.hibernate.spatial.dialect.mysql.MySQL56InnoDBSpatialDialect